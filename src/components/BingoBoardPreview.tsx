// styling:
import { css, jsx } from "@emotion/react";
import styled from "@emotion/styled";

const Container = styled.div``;

type TemplateProps = {};

const Template = ({}: TemplateProps) => {
  return <Container>BINGO BOARD</Container>;
};

export default Template;
