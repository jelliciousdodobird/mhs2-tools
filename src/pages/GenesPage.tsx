// styling:
import { css, jsx, Theme, useTheme } from "@emotion/react";
import styled from "@emotion/styled";
import { rgba } from "emotion-rgba";

const GenesPage = () => {
  return <div>Genes</div>;
};

export default GenesPage;
